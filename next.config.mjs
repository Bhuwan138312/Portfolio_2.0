/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow importing PDF and other assets from src/assets via standard imports
  // Next.js handles static assets in public/ at root URL automatically
};

export default nextConfig;
